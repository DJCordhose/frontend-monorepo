server {
    location /counter/ {
        proxy_pass http://counter:80/;
    }

    location /mobx/ {
        proxy_pass http://mobx:80/;
    }

    location / {
        root   /usr/share/nginx/html;
        index  index.html;
    }
}